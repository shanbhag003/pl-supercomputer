# Pinned deliberately: the job installs fresh on every run, so an unpinned
# major release could break a Tuesday morning publish with no change from us.
pandas==2.2.3
numpy==2.1.3
scipy==1.14.1
matplotlib==3.9.2
requests==2.32.3
pyarrow==18.1.0
